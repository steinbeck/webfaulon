# WebFaulon Backend

Python backend for SA-driven molecular structure generation.

## Development

Install dependencies:
```bash
poetry install
```

Run the server:
```bash
poetry run uvicorn app.main:app --reload
```

Run tests:
```bash
poetry run pytest
```
